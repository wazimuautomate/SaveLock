package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val SaveLockPrimary = Color(0xFF0B6E4F)      // Deep Green ("saved/success")
val SaveLockPrimaryDark = Color(0xFF074D36)  // Darker Green
val SaveLockAmber = Color(0xFFE0A526)        // Warm Amber ("pending")
val SaveLockRed = Color(0xFFC0392B)          // Soft Red ("missed/locked")

// Sleek, high-contrast Dark theme colors
val DarkBackground = Color(0xFF0B0E0D)       // Premium solid off-black/green
val DarkSurface = Color(0xFF131A17)          // Elevate cards with slate green tint
val DarkSurfaceVariant = Color(0xFF1B2521)   // Outer borders and divider backgrounds
val DarkOnBackground = Color(0xFFE6EAE7)     // Off-white readable text
val DarkOnSurface = Color(0xFFFFFFFF)        // Pure white headers
val DarkOnPrimary = Color(0xFFFFFFFF)

// Sleek Light theme colors
val LightBackground = Color(0xFFF3F6F4)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE8ECE9)
val LightOnBackground = Color(0xFF0F1512)
val LightOnSurface = Color(0xFF0F1512)
val LightOnPrimary = Color(0xFFFFFFFF)
