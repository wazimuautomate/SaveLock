<?xml version="1.0" encoding="utf-8"?>
<!--
  Config for the SaveLock app-blocker. We only need to know WHICH app came to the foreground
  (window state changes) — we never read screen content (canRetrieveWindowContent="false"),
  keeping the permission footprint minimal and privacy-respecting.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="100"
    android:description="@string/accessibility_description" />
